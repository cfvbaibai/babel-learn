const Calculator = {
  add: (a, b) => a + b,

  minus: (a, b) => a - b,

  times: function(a, b) {
    return a * b;
  },

  divide: function(a, b) {
    return a /b;
  },
};

export default Calculator;
